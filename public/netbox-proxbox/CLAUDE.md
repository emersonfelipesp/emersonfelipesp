# public/netbox-proxbox/

## Purpose
Static assets for the NetBox-Proxmox plugin project page (`/netbox-proxbox`). Currently contains only the `screenshots/` subdirectory.

## Subdirectories

- `screenshots/` — PNG screenshots of the `netbox-proxbox` plugin UI (see `screenshots/CLAUDE.md`)
